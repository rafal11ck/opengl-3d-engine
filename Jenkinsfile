pipeline {
    agent {
        docker {
            image 'archlinux'
        }
    }
    stages{
        stage('Install dependencies'){
            steps{
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}
